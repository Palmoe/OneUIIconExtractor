# Copy this file from config/keystore.properties.example to keystore.properties and replace every value with your real release signing values.
# storeFile can be either an absolute path or a path relative to the project root.
storeFile=release.keystore
storePassword=replace-with-your-store-password
keyAlias=release
keyPassword=replace-with-your-key-password
